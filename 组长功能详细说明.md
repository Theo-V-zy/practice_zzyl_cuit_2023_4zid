# 组长功能详细说明

> 分支：`feature/leader-base-auth` ｜ 合并到 `dev` 后供全组使用

---

## 总览

| 编号 | 功能模块 | 类别 |
|------|---------|------|
| 1 | 后台导航布局 | 公共基础 |
| 2 | 登录认证 | 公共基础 |
| 3 | 权限配置（用户/角色/菜单/部门/职位） | 业务功能 |
| 4 | 角色菜单权限控制 | 业务功能 |
| 5 | 工作台 Dashboard | 业务功能 |
| 6 | 消息中心 | 业务功能 |
| 7 | 个人信息 + 头像上传 | 业务功能 |
| 8 | AI 智能问答 | 业务功能 |
| 9 | 家属端小程序 | 业务功能 |
| 10 | 组员集成（来访/入退/在住） | 集成 |

---

## 1. 后台导航布局

### 需求

顶部一级模块导航栏 + 左侧二级/三级菜单侧边栏。"个人中心"和"消息中心"不在侧边栏显示，通过头像下拉和铃铛访问。

### 实现

**前端**：`AdminLayout.vue` 承载整体布局，使用 CSS Grid 分为侧边栏 + 内容区。

菜单数据有两个来源：
1. **前端原型** → `adminNavigation.js` 写死默认菜单结构
2. **后端动态** → `/sysMenus` 接口返回按角色过滤后的菜单树，**如果后端返回了非空数据则替换前端原型**

```javascript
// AdminLayout.vue - loadMenus()
function loadMenus() {
  getMenus().then((data) => {
    if (useServerMenus(data)) {
      const hideFromSidebar = ['个人中心', '消息中心']
      menuList.value = data
        .filter((item) => !hideFromSidebar.includes(item.mname))
        .sort((a, b) => (a.sort || 0) - (b.sort || 0))
    }
  })
}
```

顶栏模块 = `menuList` 中 `topVisible !== false` 的项。侧边栏 = 当前选中顶栏模块的子菜单。

**关键文件**：
- [frontend/src/layouts/AdminLayout.vue](frontend/src/layouts/AdminLayout.vue)
- [frontend/src/config/adminNavigation.js](frontend/src/config/adminNavigation.js)

---

## 2. 登录认证

### 需求

管理端登录，Session 认证，登录后 7 天有效。

### 实现

**后端**：`LoginController` 接收账号密码 → `UserServiceImpl.queryUserService()` → 查 `t_user` 表验证 → 加载角色/部门/职位信息 → 存入 `session.setAttribute("online", userLineDto)` → 返回 `{code:200}`。

密码明文比对（`dbPwd.equals(userDto.getUpwd())`）。支持账号或邮箱登录。

**前端**：`HomeView.vue` 登录页 → `POST /login` → 成功后立即调 `/loadInfo` 缓存用户信息到 `localStorage`（含头像URL），确保 AdminLayout 挂载时头像秒显。

```javascript
// HomeView.vue - handleLogin()
loadInfo().then(data => {
  if (data) localStorage.setItem('adminUser', JSON.stringify(data))
}).finally(() => router.replace('/MainIndex'))
```

```javascript
// AdminLayout.vue - loadUserInfo()
// 先从缓存秒开
const cached = localStorage.getItem('adminUser')
if (cached) { ... }
// 再从接口刷新
loadInfo().then((data) => { ... localStorage.setItem('adminUser', JSON.stringify(data)) })
```

**关键文件**：
- [backend/.../controller/LoginController.java](backend/src/main/java/com/soft/controller/LoginController.java)
- [backend/.../service/impl/UserServiceImpl.java](backend/src/main/java/com/soft/service/impl/UserServiceImpl.java)
- [frontend/src/views/HomeView.vue](frontend/src/views/HomeView.vue)

---

## 3. 权限配置（CRUD 五件套）

### 需求

用户管理、角色管理、菜单管理、部门管理、职位管理，标准 CRUD（分页查询 + 新增 + 编辑 + 删除 + 状态切换）。

### 实现

统一模式：

```
Entity(@TableName) → Mapper(BaseMapper<T>) → Service(IService<T>) → Controller(@RestController)
                                                                        ↓
                                                              返回 Map<String,Object>
                                                              {code, msg, data, total}
```

前端统一模式：

```
el-form(查询) → el-button(新增) → el-table(列表) → el-pagination(分页) → el-dialog(表单弹窗)
```

分页统一 `[10, 20, 30, 40]`。状态切换不弹确认窗。

| 模块 | 表 | 后端 Controller | 前端页面 |
|------|-----|----------------|---------|
| 用户管理 | `t_user` | `UserController.java` | `admin/UserManage.vue` |
| 角色管理 | `t_role` | `RoleController.java` | `admin/RoleManage.vue` |
| 菜单管理 | `t_menu` | `MenuController.java` | `admin/MenuManage.vue` |
| 部门管理 | `t_department` | `DepartmentController.java` | `admin/Department.vue` |
| 职位管理 | `t_post` | `PostController.java` | `admin/Post.vue` |

**关键文件**：`backend/.../controller/` 下对应 5 个 Controller，`frontend/src/views/admin/` 下对应 5 个 Vue 页面。

---

## 4. 角色菜单权限控制

### 需求

不同角色看到不同菜单。在角色管理页勾选菜单，保存后生效。

### 实现

**数据存储**：`t_role.menu_ids` 字段，逗号分隔菜单 ID（如 `"1,2,3,9,10"`）

**完整数据流**：

```
┌─ 角色管理页勾选菜单 → POST /roles/{id}/menus → 写入 t_role.menu_ids
│
├─ 用户登录 → UserServiceImpl → 查 t_role.menu_ids → 存入 session["online"].menuIds
│
└─ 前端加载菜单 → GET /sysMenus → MenuController.sysMenusList()
       │
       ├─ 读取 session["online"].menuIds
       ├─ 管理员(menu_ids含1-24全部) → 返回全部菜单
       └─ 其他人 → 解析逗号分隔ID → filterMenus() 过滤
              │
              ├─ 拍平树为 Map<id, Menu>
              ├─ 收集保留ID：允许的 + 祖先链（向上追溯 pid 直到根）
              └─ 重建过滤后的树
```

**过滤算法核心**（[MenuController.java:48-72](backend/src/main/java/com/soft/controller/MenuController.java)）：

```java
// 1. 拍平树
flattenTree(allMenus, flatMap);
// 2. 补祖先节点：如果允许菜单20(用户信息)，自动补19(权限配置)到保留集
for (Integer id : allowedIds) {
    Menu m = flatMap.get(id);
    while (m != null && m.getPid() != null && m.getPid() != 0) {
        keepIds.add(m.getPid());
        m = flatMap.get(m.getPid());
    }
}
// 3. 从原始树中只保留 keepIds 中的节点，递归过滤子节点
```

**角色切换时树勾选同步**（[RoleManage.vue:111-119](frontend/src/views/admin/RoleManage.vue)）：

```javascript
function selectRole(r){
  const ids = r.menuIds ? r.menuIds.split(',').map(Number).filter(id=>id) : []
  // 用 setCheckedKeys 强制刷新，default-checked-keys 只在首次渲染生效
  menuTreeRef.value.setCheckedKeys([])
  setTimeout(() => menuTreeRef.value.setCheckedKeys(ids), 50)
}
```

**关键文件**：
- [backend/.../controller/MenuController.java](backend/src/main/java/com/soft/controller/MenuController.java)
- [backend/.../controller/RoleController.java:86-98](backend/src/main/java/com/soft/controller/RoleController.java)
- [backend/.../service/impl/UserServiceImpl.java:74-86](backend/src/main/java/com/soft/service/impl/UserServiceImpl.java)
- [frontend/src/views/admin/RoleManage.vue](frontend/src/views/admin/RoleManage.vue)

---

## 5. 工作台 Dashboard

### 需求

展示统计数据卡片 + 待办列表 + 老人分布图表 + 营收趋势图表。

### 实现

**后端**：`DashboardController` 用 `JdbcTemplate` 执行 SQL 聚合查询。

```java
// 统计卡片数据
data.put("elderCount", jdbcTemplate.queryForObject(
    "SELECT COUNT(*) FROM t_elder WHERE status='IN'", Integer.class));
data.put("bedUsed", jdbcTemplate.queryForObject(
    "SELECT COUNT(*) FROM t_bed WHERE status='OCCUPIED'", Integer.class));
data.put("monthRevenue", jdbcTemplate.queryForObject(
    "SELECT COALESCE(SUM(pay_amount),0) FROM t_order WHERE ...", Double.class));
```

**前端**：ECharts 渲染饼图（护理等级分布）和柱状图（月度营收）。多个 API 通过 `Promise.allSettled` 并行加载。

**关键文件**：
- [backend/.../controller/DashboardController.java](backend/src/main/java/com/soft/controller/DashboardController.java)
- [frontend/src/views/admin/Dashboard.vue](frontend/src/views/admin/Dashboard.vue)

---

## 6. 消息中心

### 需求

消息列表、已读/未读筛选、查看详情、标记已读、批量已读、删除。铃铛图标显示未读数。

### 实现

- `t_message` 表，字段含 `receiver_type`、`receiver_id`、`read_status`、`enabled`
- 后端 MyBatis-Plus 分页查询，软删除（`enabled=0`）
- 前端铃铛 30 秒轮询 `getMessagePage({page:1, pageSize:1, readStatus:0})` 获取未读数
- 新消息 1 分钟内显示 `NEW` 徽章

**关键文件**：
- [backend/.../controller/MessageController.java](backend/src/main/java/com/soft/controller/MessageController.java)
- [frontend/src/views/admin/Messages.vue](frontend/src/views/admin/Messages.vue)
- [frontend/src/layouts/AdminLayout.vue](frontend/src/layouts/AdminLayout.vue)（铃铛轮询逻辑）

---

## 7. 个人信息 + 头像上传

### 需求

查看/编辑个人资料，上传头像，修改密码。

### 实现

**头像上传链路**：

```
选文件 → blob 本地预览(0秒) → 后台 OSS 上传(按钮禁用)
                                    ├─ OSS 通 → 返回 OSS URL
                                    └─ OSS 不通 → 降级本地存储 → 返回本地 URL
                                                ↓
                              上传完成 → URL 暂存 form.image(未入库)
                                                ↓
                              点保存 → /updateUser → 写入 t_user.image
```

**后端 OSS 降级**（[FileController.java](backend/src/main/java/com/soft/controller/FileController.java)）：

```java
if ("oss".equalsIgnoreCase(uploadStorage) && aliyunOssUtils.isConfigured()) {
    try { return aliyunOssUtils.uploadFile(name, bytes); }
    catch (Exception e) {
        System.err.println("OSS上传失败，降级到本地存储");
    }
}
// 本地兜底
Files.write(root.resolve(name), bytes);
return "/uploads/" + name;
```

**OSS 工具**（[AliyunOssUtils.java](backend/src/main/java/com/soft/utils/AliyunOssUtils.java)）：
- 从环境变量 `OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET` 获取凭证
- 上传后拼接返回 `https://{bucket}.{endpoint}/{objectName}`

**头像秒显**：登录时预加载 → 存 localStorage → AdminLayout 挂载先读缓存 → 再调接口刷新。

**关键文件**：
- [frontend/src/views/UesrInfo.vue](frontend/src/views/UesrInfo.vue)
- [frontend/src/views/ModifyPwd.vue](frontend/src/views/ModifyPwd.vue)
- [backend/.../controller/FileController.java](backend/src/main/java/com/soft/controller/FileController.java)
- [backend/.../utils/AliyunOssUtils.java](backend/src/main/java/com/soft/utils/AliyunOssUtils.java)
- [backend/.../config/CrossOrginConfig.java](backend/src/main/java/com/soft/config/CrossOrginConfig.java)（本地 `/uploads/` 资源映射）

---

## 8. AI 智能问答

### 需求

AI 助手对话页面，流式逐字输出，支持多轮上下文对话。

### 实现

**后端 SSE 流式**（[AIChatController.java](backend/src/main/java/com/soft/controller/AIChatController.java)）：

```
用户消息 → POST /api/chat/stream
  ↓
构建 messages = [system prompt 限定养老业务, 历史10轮, 当前消息]
  ↓
HttpURLConnection 调 DeepSeek API (stream=true)
  ↓
逐行读 SSE → 提取 choices[0].delta.content → emitter.send(token)
  ↓
前端 fetch + ReadableStream → 逐字追显
```

System prompt 限定 AI 只回答养老院相关业务问题。API 兼容 OpenAI 格式，DeepSeek 直接可用。

**前端**（[AIChat.vue](frontend/src/views/admin/AIChat.vue)）：
- 消息气泡：用户靠右、AI 靠左
- `fetch()` + `ReadableStream` + `TextDecoder` 逐字显示
- 自动滚动到底部
- 发送时带历史 10 轮消息作为上下文

**配置**：`application.properties` 中 `deepseek.api.key/url/model`

---

## 9. 家属端小程序

### 需求

uni-app 家属端微信小程序：登录、首页、家人、服务、订单、探访、合同、账单。

### 实现

**位置**：`frontend-family/`

**17 个页面**：

| 功能 | 页面目录 |
|------|---------|
| 登录 | `pages/login/` |
| 首页 | `pages/home/` |
| 家人 | `pages/family/`、`pages/bind-family/`、`pages/elder-detail/` |
| 服务 | `pages/service/`、`pages/service-detail/`、`pages/order-confirm/` |
| 订单 | `pages/orders/`、`pages/order-detail/` |
| 探访 | `pages/appointments/`、`pages/appointment-create/` |
| 合同 | `pages/contracts/` |
| 账单 | `pages/bills/` |
| 我的 | `pages/mine/`、`pages/profile/` |
| 介绍 | `pages/institution/` |

- 请求封装：`api/request.js`，统一 `/family` 前缀 + `X-Family-Token` 头
- 后端：`FamilyController.java`，从 `t_family_user` 验证登录
- 测试账号：`family001` / `123456`

**启动方式**：
```bash
cd frontend-family && npm install && npm run build:mp-weixin
# 微信开发者工具导入 dist/build/mp-weixin
```

**关键文件**：
- [frontend-family/](frontend-family/)
- [frontend-family/README.md](frontend-family/README.md)
- [backend/.../controller/FamilyController.java](backend/src/main/java/com/soft/controller/FamilyController.java)

---

## 10. 组员 A 集成

### 需求

将来访管理、入退管理、在住管理的代码合并到组长分支。

### 实现方式

1. 组员 A 的**独有新文件**（33 个）直接复制到组长分支——包括 6 个 Vue 页面和 27 个 Java 后端文件
2. `router/index.js` 中对应路由从 `ModulePlaceholder` 替换为真实组件
3. `admin.js` 中追加组员 A 的 22 个 API 函数
4. 所有 P0 冲突文件（MenuController、AdminLayout、Dashboard、UserManage 等）保留组长版本

**集成的新模块**：

| 模块 | 前端页面 | 后端 Controller |
|------|---------|----------------|
| 来访管理 | `Visit.vue` | `VisitController.java` |
| 入退管理 | `Apply.vue` | `ApplyController.java` |
| 合同管理 | `Contract.vue` | `ContractController.java` |
| 床位管理 | `Bed.vue`、`SmartBed.vue`、`RoomType.vue` | `BedController.java` |
| 老人档案 | — | `ElderController.java` |

---

## 全局修复项

| 修复 | 文件 | 方式 |
|------|------|------|
| JAXB 版本冲突 | `pom.xml` | `jaxb-runtime` 显式指定 `<version>2.3.3</version>` |
| MySQL 密码默认值 | `application.properties` | `${MYSQL_PASSWORD:we005218}` 加回默认值 |
| 头像预览等待 | `UesrInfo.vue` | `URL.createObjectURL()` 秒开本地预览 |
| 登录后头像闪烁 | `AdminLayout.vue` | 先读 localStorage 缓存再调接口 |
| 角色菜单树勾选不更新 | `RoleManage.vue` | `setCheckedKeys()` 强制刷新 |
| CORS 跨域 | `CrossOrginConfig.java` | `allowedOriginPatterns` + `allowCredentials(true)` |
| 分页条数 | 各 Vue 页面 | `[10, 20, 30, 40]` |
| 密码双星号 | `HomeView.vue`、`ModifyPwd.vue` | `type="password" show-password` |

---

## 数据库（21 张表）

| 有实体 | 无实体（JdbcTemplate 直查） |
|--------|---------------------------|
| `t_user`, `t_role`, `t_menu`, `t_department`, `t_post`, `t_message`, `t_nursimg_item`, `t_nursing_plain`, `t_plain_item`, `t_family_user` | `t_apply`, `t_bed`, `t_bill`, `t_contract`, `t_customer`, `t_device`, `t_elder`, `t_family_elder`, `t_nursing_task`, `t_order`, `t_visit` |

`t_user.image` 存头像 OSS URL。`t_role.menu_ids` 存逗号分隔的菜单权限。

---

## 关键配置（application.properties）

```properties
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/zzyl
spring.datasource.password=${MYSQL_PASSWORD:we005218}
aliyun.oss.endpoint=https://oss-cn-chengdu.aliyuncs.com
aliyun.oss.bucketName=zzyl-ziyaowei
deepseek.api.key=${DEEPSEEK_API_KEY}
deepseek.api.url=https://api.deepseek.com/v1
deepseek.model=deepseek-chat
```
